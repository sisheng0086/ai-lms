# Copy this file to .env and fill in your values
DB_HOST=localhost
DB_PORT=5432
DB_NAME=AI_lms_db
DB_USER=postgres
DB_PASSWORD=your_password_here

# OR use a full connection URL (Railway provides this automatically)
# DATABASE_URL=postgresql://user:password@host:port/dbname
